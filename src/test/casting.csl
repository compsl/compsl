
declare
{
	int a;
	float b, c;
} 

cubbyhole init 
{
        test_reset();
	b = 3.5;
        b = (int)b;
        testeqf(3,b);

        a = 3;
        a = (float)a;
        testeqi(3,a);
        testeqf(3,a);

        b = 3.4;
        testeqf(b,(float)b);

        testeqi(a,(int)a);
        testeqi(a,(float)a);

        test_summary();
} 


cubbyhole frame
{
  
}

cubbyhole point
{ 
}
