

// testing DECLS


declare
{
	int fsdf;
	a = b; // should generate syntax error
}

cubbyhole init
{// this should generate errors
	float asdf;
	int sadf;
	float sadf,s_sdafsf;
	global int sadfs,sfasdf,sadfsfd,sadf;
	global float ya;
}

